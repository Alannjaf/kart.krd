import SignOutPage from '../[path]/SignOutPage';

export default function SignOut() {
  return (
    <main className="min-h-screen bg-[var(--color-bg)] flex items-center justify-center p-4">
      <SignOutPage />
    </main>
  );
}